#include <bits/stdc++.h>
using namespace std;

int main() {
    int n1,n2;
    int ans=0;
    cin>>n1>>n2;
    for (int i=0;i<n1;i++){
        int n;
        cin>>n;
        if (n<=n2)
        ans++;
        else{
        ans+=ceil((double)n/n2);}
    }
    cout<<ans;
    return 0;
}
