#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin>>s;
    string s2="olleh";
    for (int i=0;i<s.size();i++){
        if (s[i]==s2.back())
        s2.pop_back();
    }
    if (s2=="")
    cout<<"YES";
    else
    cout<<"NO";
    return 0;
}
